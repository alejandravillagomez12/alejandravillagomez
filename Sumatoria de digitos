#include <iostream>
using namespace std;
int main()
{
	    int N, d; 
		float suma=0;
		cout << "Ingrese el numero: ";
		cin >> N;
		suma = 0
			while (N > 0) {
				d = N % 10;
				N = N / 10;
				suma = suma + d;
			}
		cout << "La suma de los digitos de: " << N << " es : " << suma;
		
}
